import { Column, CreatedAt, DataType, Model, Scopes, Table, UpdatedAt } from 'sequelize-typescript';
const defaultAttributes = ['id',{{attributesArr}}];
const fullAttributes = defaultAttributes.concat(['createdAt', 'updatedAt']);
// only for debugging
// import { dd } from "../Sys/Debug";

@Scopes({
	default: { attributes: defaultAttributes },
	full: { attributes: fullAttributes },
})
@Table({
	tableName: '{{tableName}}',
})
export class {{Module}} extends Model<{{Module}}> {
	// UUID Type
	@Column({
		allowNull: false,
		primaryKey: true,
		type: DataType.UUID,
		defaultValue: DataType.UUIDV4,
	})
	id: string;

	// integer Type
	/*
	@Column({
		allowNull: false,
		primaryKey: true,
		autoIncrement: true,
		type: DataType.BIGINT,
	})
	id: number;
	*/

{{attrDeclarations}}
	@CreatedAt
	createdAt: Date;

	@UpdatedAt
	updatedAt: Date;
}

